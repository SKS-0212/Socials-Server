meta {
  name: create account
  type: http
  seq: 3
}

post {
  url: {{social_local}}/auth/createAccount
  body: json
  auth: bearer
}

auth:bearer {
  token: {{account_token}}
}

body:json {
  {
    "name":"Nikhil",
    "password":"Nik123",
    "username":"nik@123"
  }
}

script:post-response {
  let token = res.getBody()?.data?.accessToken;
  bru.setEnvVar("token", token);
}
