meta {
  name: verify forgot password otp
  type: http
  seq: 7
}

post {
  url: {{social_local}}/auth/verifyForgotPasswordOTP
  body: json
  auth: inherit
}

body:json {
  {
    "email":"malhotranikhil863@gmail.com",
    "otp":"123456"
  }
}

script:post-response {
  let token = res.getBody()?.data?.token;
  bru.setEnvVar("reset_token", token);
}
