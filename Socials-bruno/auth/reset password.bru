meta {
  name: reset password
  type: http
  seq: 8
}

post {
  url: {{social_local}}/auth/resetPassword
  body: json
  auth: bearer
}

auth:bearer {
  token: {{reset_token}}
}

body:json {
  {
    "password":"groot"
  }
}

script:post-response {
  let token = res.getBody()?.data?.accessToken;
  bru.setEnvVar("token", token);
}
