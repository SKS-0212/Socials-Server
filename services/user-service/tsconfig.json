{
    "extends": "../../tsconfig.json",
    "compilerOptions": {
        "outDir": "dist",
        "rootDir": "src",
        "typeRoots": [
            "./node_modules/@types",
            "./src/types" // If using /types directory
            // "./@types"      // If using /@types directory
        ]
    },
    "include": [
        "src"
    ]
}